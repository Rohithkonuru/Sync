{
  "name": "sync-fullstack",
  "version": "1.0.0",
  "description": "Sync - Professional Networking Platform",
  "scripts": {
    "build": "cd frontend && npm run build",
    "start": "cd frontend && npm start",
    "install": "cd frontend && npm install"
  },
  "engines": {
    "node": ">=16.0.0"
  },
  "dependencies": {
    "framer-motion": "^12.23.26"
  }
}
